import React from 'react'

export const SiteAlert = (): React.ReactElement => {
  return (
    <section data-testid="siteAlert">
      <div>some text</div>
    </section>
  )
}

export default SiteAlert
