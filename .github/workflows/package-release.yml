name: Package release

on:
  pull_request:
    types: 
      - closed
    branches: 
      - main

jobs:
  package-release:
    runs-on: ubuntu-latest
    if: |
      startsWith(github.head_ref, 'release-')
      && github.event.pull_request.merged

    strategy:
      fail-fast: false

    steps: 
      - name: Checkout out repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Set up Node
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version-file: '.node-version'
          cache: 'npm'

      # The npm cache is not node_modules
      - name: Install Dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Package library
        # https://docs.npmjs.com/cli/commands/npm-pack
        run: npm pack

      - name: Upload package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          path: trussworks-react-uswds-*.tgz
          if-no-files-found: error
          retention-days: 5
          # Upload the `npm pack` tarball output as-is rather than wrapping it in a zip, so it can be
          # downloaded and published directly.
          archive: false
