import { default as React } from 'react';

declare const _default: {
    title: string;
    component: ({ id, className, mask, value: externalValue, defaultValue, charset, onChange, ...inputProps }: import('./TextInputMask').TextInputMaskProps) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
    parameters: {
        docs: {
            description: {
                component: string;
            };
        };
    };
};
export default _default;
export declare const Default: () => React.ReactElement;
