import React from '../../../node_modules/react';
interface CollectionCalendarDateProps {
    datetime: string;
}
export declare const CollectionCalendarDate: ({ datetime, }: CollectionCalendarDateProps) => React.ReactElement;
export {};
