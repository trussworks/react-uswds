import React from '../../../../node_modules/react';
type InputPrefixProps = {
    className?: string;
    children: React.ReactNode;
} & JSX.IntrinsicElements['div'];
export declare const InputPrefix: ({ className, children, ...divProps }: InputPrefixProps) => React.ReactElement;
export {};
