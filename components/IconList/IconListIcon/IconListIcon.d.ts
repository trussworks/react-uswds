import { ReactElement, ReactNode } from 'react';
interface IconListIconProps {
    className?: string;
    children: ReactNode;
}
export declare const IconListIcon: ({ className, children, ...divProps }: IconListIconProps & JSX.IntrinsicElements['div']) => ReactElement;
export default IconListIconProps;
