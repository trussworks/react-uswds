import React from '../../../node_modules/react';
type SideNavProps = {
    items: React.ReactNode[];
    isSubnav?: boolean;
};
export declare const SideNav: ({ items, isSubnav, }: SideNavProps) => React.ReactElement;
export {};
